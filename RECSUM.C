#include<stdio.h>
#include<conio.h>
int addnumbers (int);
void main()
{
	int num;
	clrscr();
	printf("Enter a positive integer:");
	scanf("%d",&num);
	printf("sum=%d", addnumbers(num));
	getch();
}
	int add numbers (int x)
{
	int(n!=0)
	return n + add numbers(n-1);
	else
	return(n);
}